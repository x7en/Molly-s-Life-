# Memories folder

Place uploaded memory images in this directory using the naming pattern `memory-##.jpg` (for example, `memory-1.jpg`, `memory-2.jpg`, etc.). The game will load images from this folder before falling back to built-in placeholders.
